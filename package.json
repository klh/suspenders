{
  "name": "suspenders",
  "version": "0.1.0",
  "description": "A governor for agent fleets — SQLite control plane, capability-aware dispatch, zombie lanes, decision forks with LLM advice, live fleet board",
  "type": "module",
  "scripts": {
    "test": "bun test test/",
    "ci": "bun install && bun test test/"
  },
  "dependencies": {
    "shell-quote": "^1.8.2"
  },
  "engines": {
    "bun": ">=1.1"
  }
}
